# This file is autogenerated by katenary
#
# DO NOT EDIT IT BY HAND UNLESS YOU KNOW WHAT YOU ARE DOING
#
# If you want to change the content of this file, you should edit the
# compose file and run katenary again.
# If you need to override some values, you can do it in a override file
# and use the -f flag to specify it when running the helm command.



# backend-api configuration
backend-api:
  # key: backend-api.repository
  repository:
    # key: backend-api.repository.image
    image: hummingbot/backend-api
    # key: backend-api.repository.tag
    tag: latest
  # key: backend-api.replicas
  replicas: 1
  # key: backend-api.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: backend-api.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: backend-api.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: backend-api.serviceAccount
  serviceAccount: ""

# dashboard configuration
dashboard:
  # key: dashboard.repository
  repository:
    # key: dashboard.repository.image
    image: hummingbot/dashboard
    # key: dashboard.repository.tag
    tag: latest
  # key: dashboard.replicas
  replicas: 1
  # key: dashboard.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: dashboard.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: dashboard.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: dashboard.serviceAccount
  serviceAccount: ""

# emqx configuration
emqx:
  # key: emqx.repository
  repository:
    # key: emqx.repository.image
    image: emqx
    # key: emqx.repository.tag
    tag: "5"
  # key: emqx.persistence
  persistence:
    # key: emqx.persistence.emqx_data
    emqx_data:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: emqx.persistence.emqx_data.storageClass
      storageClass: '-'
      # key: emqx.persistence.emqx_data.size
      size: 1Gi
      # key: emqx.persistence.emqx_data.accessMode
      accessMode:
        - ReadWriteOnce
      # key: emqx.persistence.emqx_data.enabled
      enabled: true
    # key: emqx.persistence.emqx_etc
    emqx_etc:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: emqx.persistence.emqx_etc.storageClass
      storageClass: '-'
      # key: emqx.persistence.emqx_etc.size
      size: 1Gi
      # key: emqx.persistence.emqx_etc.accessMode
      accessMode:
        - ReadWriteOnce
      # key: emqx.persistence.emqx_etc.enabled
      enabled: true
    # key: emqx.persistence.emqx_log
    emqx_log:
      # Storage class to use for PVCs
      # storageClass: "-" means use default
      # storageClass: "" means do not specify
      # storageClass: "foo" means use that storageClass
      # key: emqx.persistence.emqx_log.storageClass
      storageClass: '-'
      # key: emqx.persistence.emqx_log.size
      size: 1Gi
      # key: emqx.persistence.emqx_log.accessMode
      accessMode:
        - ReadWriteOnce
      # key: emqx.persistence.emqx_log.enabled
      enabled: true
  # key: emqx.replicas
  replicas: 1
  # key: emqx.nodeSelector
  nodeSelector: {}
  # Resources allows you to specify the resource requests and limits for a service.
  # Resources are used to specify the amount of CPU and memory that 
  # a container needs.
  #
  # e.g.
  # resources:
  #   requests:
  #     memory: "64Mi"
  #     cpu: "250m"
  #   limits:
  #     memory: "128Mi"
  #     cpu: "500m"
  # key: emqx.resources
  resources: {}
  # imagePullPolicy allows you to specify a policy to cache or always pull an image.
  # You must provide a string value with one of the following values:
  # - Always       -> will always pull the image
  # - Never        -> will never pull the image, the image should be present on the node
  # - IfNotPresent -> will pull the image only if it is not present on the node
  # key: emqx.imagePullPolicy
  imagePullPolicy: IfNotPresent
  # key: emqx.serviceAccount
  serviceAccount: ""

# imagePullSecrets allows you to specify a name of an image pull secret.
# You must provide a list of object with the name field set to the name of the
# e.g.
# pullSecrets:
# - name: regcred
# You are, for now, responsible for creating the secret.
pullSecrets: []

# vim: ft=yaml
